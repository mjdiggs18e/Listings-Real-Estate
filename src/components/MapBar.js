import React from "react";
import styled from "styled-components";

const MapbarContainer = styled.div`
  width: 50vw;
  background-color: #f8f9fa;
`;

const MapBar = () => {
  return <MapbarContainer></MapbarContainer>;
};

export default MapBar;
